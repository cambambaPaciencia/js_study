
export function generateEstudo(){
    return (Math.random()*4).toFixed(0);
}

export function generateSono(){
    return (Math.random()*8).toFixed(0);
}

export function generateTelefone(){
    return (Math.random()*10).toFixed(0);
}

export function generateDistancia(){
   return (Math.random()*10).toFixed(0);
}